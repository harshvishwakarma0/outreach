# Supabase Project And Tables

Use this logical app database label in your notes:

`whatsapp_outreach`

Supabase stores this inside your project Postgres instance.

Required tables for this app:

- `app_users`
- `user_data`

Create them by running `supabase_schema.sql` in Supabase SQL Editor.
